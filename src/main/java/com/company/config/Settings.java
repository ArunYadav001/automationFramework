package com.company.config;

public class Settings {
}
/**
 * A one place to configure / control the framework should be the configuration file.
 * Its up to the person who is implementing the framework how he configure the framework
 * It depends on the implementation of the framework and hardcoded values must be avoided
 *
 *
 * Configuration should have the required information to control the framework or giving some details which can be
 * used as a record for the test session
 */